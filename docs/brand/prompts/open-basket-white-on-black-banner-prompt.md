# Open Basket — white-on-black banner

Tool mode: built-in Imagegen

Requested output: 3840 × 1280 pixels. Actual native output: 2172 × 724 pixels (3:1), verified with macOS sips. No resizing was applied.

Edit target: `open-basket-logo-v2.png`

## Exact prompt

Use case: precise-object-edit; logo-brand
Asset type: final flat brand asset for Open Basket, a social profile header and GitHub README banner, requested 3840 x 1280 pixels, 3:1 landscape.
Input image 1 is the edit target and the exact brand identity source, not merely loose inspiration.
Primary request: extract and faithfully reproduce ONLY the central large horizontal logo lockup from the reference: the distinctive geometric basket symbol followed by the exact lowercase words "open basket", recolored pure white on a completely solid pure black background.
Invariants: Preserve the exact silhouette of the reference mark, all three filled masses, the two asymmetric diagonal negative-space cuts, the rounded lower corners, and the tall open top. Preserve the exact heavy geometric sans-serif lowercase letterforms, including single-story a, widths, weight, kerning and the relative symbol-to-wordmark proportions. Do not redesign or substitute any symbol.
Composition: One single horizontal logo lockup, centered optically and vertically in a generous 3:1 banner. Logo lockup about 72% of canvas width with generous empty border on all sides. Maintain the same symbol-to-text arrangement. All visible text must be exactly "open basket".
Palette: only white #FFFFFF for the symbol and letters and black #000000 for the background. Flat perfectly uniform surfaces, with crisp antialiased edges. High-resolution clean production branding.
Remove: the two small example icon tiles from the bottom of the reference, all yellow, all paper texture, all shadows, all gradients, all 3D, all lighting, all mockups, all borders, all small captions and all decorations. Entire empty background must be uniform black. Render only ONE logo on ONE black canvas.
